[workspace]
# release.yml builds binaries first, then creates the GitHub release with all
# assets attached at once via `gh release create`. release-plz only publishes
# to crates.io and pushes the tag.
git_release_enable = false
